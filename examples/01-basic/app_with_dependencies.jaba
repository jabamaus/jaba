cpp :BasicAppWithDependencies do
  root '../ExampleApp'
  type :console
  hosts [:vs2019], platforms: [:windows_x86, :windows_x86_64]
  configs [:Debug, :Release]
  src ['main.cpp']
  deps [:BasicStaticLib, :BasicDll]
  defines ['_CONSOLE'] # TODO into jaba
end
